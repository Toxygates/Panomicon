#This is the main configuration file that we use, despite the name. It is called log4j-test to override the bundled
#log4j.properties in intermine-resources, according to log4j's configuration resolution rules (this was the simplest way).

log4j2.debug=true

appenders = console

appender.console.type = Console
appender.console.name = STDOUT
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = %m%n
appender.console.filter.threshold.type = ThresholdFilter
appender.console.filter.threshold.level = error

rootLogger.level = info
rootLogger.appenderRef.stdout.ref = STDOUT
